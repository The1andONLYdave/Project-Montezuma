//new code to implement in v0.0.4
//setTileProperties(4, WALL);
//setTileProperties(5, FLOOR);


//setTile to make them by coordinates x and y go away and setTileProperties to make them walk-through(no collision)

//setTileProperties(1, FlxObject.ANY, onTileHit);
//FlxG.collide(hero, tilemap);

//private function onTileHit(hero:FlxSprite, tile:FlxTile):void{
//make all questionmark(?) tiles go away if you jump up against the exclemationmark(!) box in the same color
//setTileProperties(NUMBERquestionmark, FlxObject.NONE); //ok or better "make them no collision than you can walk through"
//}